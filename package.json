{
  "name": "spxcel",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "echo \"Run apps individually: pnpm -F web dev | pnpm -F admin-backend start | pnpm -F admin-frontend dev\"",
    "vercel-build": "pnpm --filter web... build",
    "lint": "echo \"Add root lint if needed\""
  }
}
